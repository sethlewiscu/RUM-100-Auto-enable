{
  "name": "rum-auto-approve",
  "version": "1.0.0",
  "description": "Receives ClickUp taskCreated webhooks and auto-creates + approves Split.io (Harness) RUM segment change requests.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "LOG_LEVEL=debug node --env-file=.env --watch src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
